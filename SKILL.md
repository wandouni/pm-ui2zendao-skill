---
name: pm-ui2zendao-skill
description: >
  电力系统产品经理专用 Skill。
  输入模糊需求描述或图片界面，输出结构化界面需求文档。
  需求文档与界面原型通过编号互相对应。
  Use when: 用户输入界面需求、功能描述、字段列表，或要求生成/更新界面需求文档。
---
# 电力系统 PM 界面需求 Skill

## 工作流

1. 分析输入（文字描述 / 界面截图）
2. 输出【需求规格文档】
3. 需求有待确认项时在输出文档前向用户逐一询问，直到没有疑问了再输出需求文档
4. 需求文档支持输出txt格式文件及word文件，输出文件前向客户询问需要哪种

---

## 需求文档规范

### 结构规则

- 按人看界面的视觉顺序（从上到下、从左到右）组织层级
- 一级编号（1、2、3）对应页面主元素（标题、工具栏、表格…）
- 二级编号（1.1、1.2）对应元素内的子功能或字段
- 三级编号（1.1.1）用于需要进一步拆分的交互细节
- 不描述样式（颜色、字号、间距、宽度）
- 不描述布局位置（由设计规范统一管控）
- 只描述：内容、数据、交互逻辑
- 功能名、按钮名、页面名一律用【】标注

### 空行规则

- 一级标题之间空一行
- 一级标题内部（二级、三级）不空行

### 缩进规则

- 1 级编号：无缩进；描述行缩进 2 空格
- 2 级编号：缩进 2 空格；描述行缩进 4 空格
- 3 级编号：缩进 4 空格；描述行缩进 4 空格（与编号同级）
- 其他层级依此类推

### 表格列描述顺序

按界面从左到右的列顺序逐一描述，每列包含：

- 数据内容说明
- 表头筛选/排序规则（见下方自动规则）
- 固定方向（如有）
- 点击/交互行为（如有）

## 表格通用规则

### 表头筛选自动规则

| 列类型                         | 自动添加              |
| ------------------------------ | --------------------- |
| 名称类文本列                   | 文本搜索框            |
| 枚举值列（状态、类型、等级等） | 下拉筛选              |
| 日期列                         | 升/降序排序，默认降序 |
| 纯文本列（创建人等）           | 无控件                |

### 固定列规则

- 列较多时（超过 5 列可滚动）：名称列自动固定左侧，操作列自动固定右侧，中间列横向滚动
- 固定列与滚动区之间显示阴影分隔，滚动到边界时阴影消失
- 序号列始终固定左侧

### 列设置

列较多时工具栏自动增加【列设置】入口：

- 非固定列：支持显隐勾选、拖拽排序
- 固定列：标注「固定左/右」，不可操作
- 提供【重置】【全部显示】快捷操作

### 操作列

- 图标按钮样式，hover 显示中文 tooltip
- 固定右侧
- 按钮顺序从左到右：编辑类 → 解析/处理类 → 复制类 → 删除类 → 下载类

### 批量操作

- 表格左侧自动添加复选框列
- 勾选一条或多条后，工具栏出现对应批量操作按钮
- 批量删除需二次确认弹窗，文案：「删除后不可恢复，确认删除已选 {N} 条记录？」

### 弹窗按钮顺序

- 危险操作（删除）：【取消】在左，【确认删除】在右（红色）
- 普通操作（另存为、编辑）：【取消】在左，【确认】在右（主色）

### 分页器

位于表格右下角，包含：条数统计、每页条数切换（10 / 20 / 50）、页码导航、跳转输入框

---

## 输出示例参考

详见 `references/example-bpa-case.md`
